# Results directory

This directory holds the outputs of `scripts/run_pipeline.py`,
`scripts/run_benchmarks.py`, and `scripts/make_figures.py`.

Contents are gitignored — they are regenerated on demand.
